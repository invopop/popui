{
  "name": "@invopop/popui",
  "license": "MIT",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:tailwind:base": "npx @tailwindcss/cli@4.1.13 -i ./styles.css -o ./dist/popui.css --minify",
    "build:tailwind:full": "npx @tailwindcss/cli@4.1.13 -i ./styles-full.css -o ./dist/popui-full.css --minify",
    "build:tailwind": "npm run build:tailwind:base && npm run build:tailwind:full"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.13",
    "@tailwindcss/forms": "^0.5.9",
    "@tailwindcss/typography": "^0.5.15"
  }
}
